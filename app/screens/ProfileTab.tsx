import { ScrollView } from "react-native"
import { Text } from "@rneui/themed"

const ProfileTab = () => {
    return (
        <ScrollView>
            <Text>Profile</Text>
        </ScrollView>
    )
}

export default ProfileTab